platform :ios, '8.0'

#屏蔽pod库所有第三方的警告
inhibit_all_warnings!

#使用swift加上，比如reactiveCocoa
use_frameworks!

target 'EmptyDataDemo' do
    
    #屏幕适配
    pod 'Masonry', '~> 1.0.2'

    #空页、出错面
    pod 'DZNEmptyDataSet'
end
